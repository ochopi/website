# ── Sistema operativo ───
.DS_Store
Thumbs.db
desktop.ini
$RECYCLE.BIN/

# ── Editores y IDEs ───
.vscode/
.idea/
*.swp
*.swo
*~

# ── Python ─────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class

# Virtual environments
.venv/
venv/
env/
ENV/

# Python tooling / caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ── Flet / build ──────────────────────────────────
dist/
build/

# ── Secretos y credenciales ───
# (La regla más importante: )captura casi cualquier archivo de secretos)
*.env
.env*
**/.env
*.secret
secrets.*
*password*
*passwd*
*token*
*apikey*
*api_key*
id_rsa
id_ed25519
*.pem
*.key
*.p12

# ── Backups y archivos temporales ───
*.bak
*.tmp
*.log
*.orig

# ── Archivos comprimidos (para no subir dumps/backups grandes) ───
*.tar
*.tar.gz
*.zip
*.gz
*.sql.gz

# ── Carpetas de datos sensibles ───
secrets/
private/
credentials/